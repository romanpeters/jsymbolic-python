# Pre-processor example input and output data

If you run the pre-processor with the `input/` directory as input, the output will be as seen in the `output/` directory and `preprocessor_changes.csv` file.
